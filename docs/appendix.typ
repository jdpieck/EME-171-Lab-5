#pagebreak()
= Appendix - Source
#set raw(block: true)
#show raw: set block(breakable: true, width: 100%)
// #show heading: show raw.where(block: false): set block(fill: white)

== main.m
#raw(read("../scripts/main.m"))

#pagebreak()
== parameters.m
#raw(read("../scripts/parameters.m"))

#pagebreak()
== initialize.m
#raw(read("../scripts/initialize.m"))

#pagebreak()
== find_time_control_parameters.m
#raw(read("../scripts/find_time_control_parameters.m"))

#pagebreak()
== road_profile.m
#raw(read("../scripts/turbine_profile.m"))

#pagebreak()
== eqns.m
#raw(read("../scripts/eqns.m"))

#pagebreak()
== display_output.m
#raw(read("../scripts/display_output.m"))
